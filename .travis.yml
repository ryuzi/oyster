language: bash

sudo: false

addons:
  apt:
    sources:
      - debian-sid
    packages:
      - shellcheck

script:
  - export SHELLCHECK_OPTS='--exclude=SC1008,SC1090,SC2034'
  - shellcheck oyster
  - ./install
  - source ~/.oysterc
  - oyster

matrix:
  fast_finish: true

