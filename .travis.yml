language: bash

sudo: false

addons:
  apt:
    sources:
      - debian-sid
    packages:
      - shellcheck

script:
  - shellcheck oyster
  - ./install
  - source ~/.oysterc
  - oyster

matrix:
  fast_finish: true

